import 'package:flutter/material.dart';

class SizeHeight10 extends StatelessWidget {
  const SizeHeight10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    );
  }
}

class SizeHeight5 extends StatelessWidget {
  const SizeHeight5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 5,
    );
  }
}
